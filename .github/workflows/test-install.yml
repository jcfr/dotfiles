name: test-install

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    name: test-install
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
      - run: make
      - run: ./aptfile
